Running hh200 in distributed manner is unfortunately necessary to execute in the news load testing. See [benchmark]() to confirm whether the single machine you're on can achieve the target number of users you have in mind. (Saving you a click: it should handle 1k comfortably on average developer laptops, but definitely not 50k.)

This page hopes to be a useful resource that helps you run hh200 at scale on your infrastructure.

## Running in Docker

## Concurrency primitives
